import * as React from 'react';
import { Button, View, Text } from 'react-native';

export const Reminders = () => {
    return (
        <View>
        <Text>Reminders</Text>
        </View>
    );
}